import { TransformEmailPipe } from './transform-email.pipe';

describe('TransformEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
